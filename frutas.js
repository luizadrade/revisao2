let frutas = ['morango',' melancia','laranja','uva'];
let quantidade = parseFloat('digite o nomes das frutas:');
for(i=0; i<quantidade; i++)
{
    let novafrutas = parseInt('digite nova frutas: '+ (i+1));
    frutas.push ('nomesfrutas');
for(i=0; i<=frutas.length; i++)
    console.log(' A posição do array ' + i + ' eh '+ [frutas[i]])
}